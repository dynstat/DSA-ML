impl Solution {
    pub fn subsets(nums: Vec<i32>) -> Vec<Vec<i32>> {
        let mut ans: Vec<Vec<i32>> = vec![vec![]];

        // for &num in &nums {
        //     let mut new_subsets = Vec::with_capacity(ans.len());
        //     for subset in &ans {
        //         let mut new_subset = subset.clone();
        //         new_subset.push(num);
        //         new_subsets.push(new_subset);
        //     }
        //     ans.extend(new_subsets);
        // }

        fn rec (mut i:usize, mut temp:Vec[i32]){

        }

        ans
    }
}
